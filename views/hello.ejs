<h1>Hello</h1>

<% if(paintings != '') { %>
    <% paintings.forEach(function(paintings, hello) { %>

    <a href="/hello/<%-paintings.name %>"></a>
       
            <img src="<%- paintings.imageURL %>" alt="<%- paintings.name %>" loading="lazy">
        </div>  
        <div class="pt-1"><%- paintings.name %></div>
    </a>     -->

    <!-- <% }) %>
<% } %>
